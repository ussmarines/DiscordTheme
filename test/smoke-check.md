# Smoke check

Manual checks after importing `themes/sibylla.theme.css` in Vencord / BetterDiscord:

- Discord UI remains visible.
- Server list is visible.
- Channel list is visible.
- Chat panel is visible.
- Member list is visible.
- User panel is visible.
- DM icon uses the Sibylla mark.
- No flavor appears in the repository.
- No runtime import points to the original refact0r GitHub.
