{
    "name": "sibnight-discord",
    "version": "2.13.1",
    "scripts": {
        "build": "node scripts/build.js",
        "check": "node scripts/check.js",
        "dev": "node scripts/dev.js",
        "prepare:release": "npm run build && npm run check",
        "test": "node --test scripts/test.js",
        "format:check": "node scripts/format-check.js",
        "bundle": "node scripts/bundle.js",
        "check:urls": "node scripts/check-urls.js",
        "quality": "npm run prepare:release && npm test && npm run format:check && npm audit --audit-level=moderate"
    },
    "author": "ussmarines",
    "license": "MIT",
    "devDependencies": {
        "chokidar": "4.0.3",
        "postcss": "8.5.28"
    },
    "description": "Sibylla-inspired Discord theme with a stable build pipeline and flavor variants.",
    "private": true,
    "engines": {
        "node": ">=24"
    }
}
